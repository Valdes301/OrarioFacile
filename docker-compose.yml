version: '3.8'

services:
  orariofacile:
    container_name: orariofacile
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
